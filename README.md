# Compiler-Construction-Project
A project of  compiler Construction for a custom programming language built in Python. It implements lexical analysis, LL(1) recursive descent parsing, semantic analysis using symbol tables and type checking, and three-address code (TAC) generation with full error handling.
